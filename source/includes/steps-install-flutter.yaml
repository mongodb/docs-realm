title: Create a Flutter App
ref: create-a-Flutter-app
content: |

  TODO... get relevant link here
  Create a Flutter app. Follow the instructions at the `Flutter documentation
  <>`__.

---
title: Add Realm to the Project
ref: add-realm-to-project
content: |

  To add the {+service-short+} Flutter SDK to your project, run the command:

  .. codeblock:: 

     flutter pub add realm

  This downloads the ``realm`` package and adds it to your project. 
  In your ``:caption: src/graphql/useProjects.js`` file, you should see:

  .. codeblock:: yaml
     :caption: src/graphql/useProjects.js

     dependencies:
       realm: ^0.1.0+preview

---
title: Import Realm Package
ref: import-realm-package
content: |

  To use the {+service-short+} Flutter SDK within your app, import the package 
  into files where you will use it:

    .. codeblock:: dart

       import 'package:realm/realm.dart';
