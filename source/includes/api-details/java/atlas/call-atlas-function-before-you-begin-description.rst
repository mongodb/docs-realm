The example on this page demonstrates calling a simple function named
``sum`` that takes two arguments, adds them, and returns the result:

.. code-block:: javascript
   :copyable: false

   // sum: adds two numbers
   exports = function(a, b) {
     return a + b;
   };
