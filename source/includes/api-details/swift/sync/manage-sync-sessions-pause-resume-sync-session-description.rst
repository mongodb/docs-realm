You can pause a Sync session by calling
``SyncSession.suspend()`` on the :swift-sdk:`SyncSession 
<Extensions/SyncSession.html>` of a synced database instance.

You can resume a Sync session with ``SyncSession.resume()``.
