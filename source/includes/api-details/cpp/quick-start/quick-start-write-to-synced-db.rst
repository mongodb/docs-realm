In this example, we store the ``user.identifier()`` of the logged-in user in
the ``ownerId`` property of the ``Todo`` item. We then use this property
in subscription queries and the Device Sync permissions.
