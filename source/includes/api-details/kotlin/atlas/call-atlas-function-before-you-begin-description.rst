For this example, we call an Atlas Function named ``sum`` that takes two
arguments, adds them, and returns the result: 

.. code-block:: javascript
   :caption: Atlas Function

   // Add two numbers
   exports = function(a, b) {
      return a + b;
   };
