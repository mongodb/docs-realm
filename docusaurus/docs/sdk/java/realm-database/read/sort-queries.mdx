---
title: Sort Queries
---
import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';
import CodeBlock from '@theme/CodeBlock';
import Intro from './_read-intro.md';
import SortKotlin from '!!raw-loader!@site/docs/generated/java/sync/ReadsTest.codeblock.sort-results.kt';
import SortJava from '!!raw-loader!@site/docs/generated/java/sync/ReadsTest.codeblock.sort-results.java';

<Intro />

A **sort** operation allows you to configure the order in
which Realm Database returns queried objects. You can sort based on
one or more properties of the objects in the results collection.

The SDK only guarantees a consistent order of results when the
results are sorted.

The following code sorts the projects by name in reverse
alphabetical order (i.e. "descending" order).

<Tabs groupId="java-sdk-languages">
    <TabItem value="kotlin" label="Kotlin">
        <CodeBlock language="kotlin">{SortKotlin}</CodeBlock>
    </TabItem>
    <TabItem value="java" label="Java">
        <CodeBlock language="java">{SortJava}</CodeBlock>
    </TabItem>
</Tabs>